<div class="account-box">
  <div class="account-maintitle">
    <i class="fa fa-file-text-o" style="padding-right:8px;"></i>编辑文章
  </div>

  <hr>
  <%= render "form" %>
  <!-- 当前封面图 -->
  <% if @article.image.present? %>
    <span>封面图</span>
    <%= image_tag(@article.image.thumb.url) %>
  <% end %>
</div>
