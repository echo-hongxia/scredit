<%= simple_form_for [:admin, @country_category] do |f| %>
  <div class="form-group">
    <%= f.input :title, :label => "分类名称" %>
    <hr>
  </div>
  <%= f.submit "提交", class: "btn btn-sm btn-default", data: {disable_with: "Submiting..."} %>
<% end %>
